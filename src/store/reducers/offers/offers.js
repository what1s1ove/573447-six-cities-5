const offers = [];
const locations = [];

const initialState = {
  offers,
  locations,
};

const gameReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export {gameReducer};

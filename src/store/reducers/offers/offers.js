const offers = [];
const locations = [];

const initialState = {
  offers,
  locations,
};

const offersReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export {offersReducer};
